# docs/process · 方法、边界与协作规则

本仓库的 process 层权威文档是 [`docs/handoff.md`](../handoff.md)（开发约定：
红线、本地门禁、环境坑、口径变更流程、验证基线）。历史原因放在 `docs/` 根下，
**不要搬迁**，引用即可。

补充约定（2026-08-26 起）：

- 跨 session 交接按版本目录走 `docs/visions/<version>/`，小改动直接更新版本 README，
  不必每次新建 handoff 文件。
- 审查类产出放 `docs/reviews/`，按日期命名，findings 必须带文件与行号。
